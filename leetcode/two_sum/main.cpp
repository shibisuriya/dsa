#include <iostream>

#include "./input_parser.h"

using namespace std;

class Solution {
     public:
      bool palindrom(int num) { return false; }
};

int main() {
      Solution solution;
      Parser parser;
      pair<string, int> inputs = parser.parse();
      return 0;
}
