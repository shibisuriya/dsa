#include <bits/stdc++.h>

using namespace std;

class Solution {
     public:
      void print(vector<int> vec) {
            for (int v : vec) {
                  cout << v << "shibi -> ";
            }
            cout << endl;
      }
};
